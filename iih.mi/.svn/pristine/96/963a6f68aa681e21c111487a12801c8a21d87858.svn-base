package iih.mi.inst.bizbase2;


import iih.mi.itf2.bizflowitf.IBdFlowItf;
import iih.mi.itf2.dto.MiResultOutParam;
import iih.mi.itf2.dto.drugmanualcomp.d.LoadOrginalDataInParam;
import iih.mi.itf2.dto.drugmanualcomp.d.LoadOrginalDataOutParam;
import iih.mi.itf2.dto.mideldircontrast.d.MiDelDirContrastInParam;
import iih.mi.itf2.dto.mideldircontrast.d.MiDelDirContrastOutParam;
import iih.mi.itf2.dto.midirautocomp.d.MiDirAutoCompInParam;
import iih.mi.itf2.dto.midirautocomp.d.MiDirAutoCompOutParam;
import iih.mi.itf2.dto.midirsave.d.MiDirSaveInParam;
import iih.mi.itf2.dto.midirsave.d.MiDirSaveOutParam;
import iih.mi.itf2.dto.misavedircontrast.d.MiSaveDirContrastInParam;
import iih.mi.itf2.dto.misavedircontrast.d.MiSaveDirContrastOutParam;
import iih.mi.itf2.dto.refreshcomp.d.MiDirRefreshCompInParam;
import iih.mi.itf2.dto.refreshcomp.d.MiDirRefreshCompOutParam;
import xap.mw.core.data.BizException;
import xap.mw.coreitf.d.FBoolean;

public class BdFlowItfImpl extends MiBaseImpl implements IBdFlowItf{

	/**
	 * 医保目录保存
	 * @author zhang.hw
	 * @date 2019年7月18日
	 * @param param
	 * @return
	 * @throws BizException
	 */
	@Override
	public MiResultOutParam<MiDirSaveOutParam> saveMiDir(MiDirSaveInParam param) throws BizException {
		MiResultOutParam<MiDirSaveOutParam> outParam = new MiResultOutParam<MiDirSaveOutParam>();
		outParam.setFg_deal(FBoolean.FALSE);
		outParam.setFg_HpSuccess(FBoolean.FALSE);
		outParam.setErrorMsg("医保目录保存 功能未实现。");
		return outParam;
	}

	/**
	 * 医保目录自动对照
	 * @author zhang.hw
	 * @date 2019年7月18日
	 * @param param
	 * @return
	 * @throws BizException
	 */
	@Override
	public MiResultOutParam<MiDirAutoCompOutParam> autoCompMiDir(MiDirAutoCompInParam param) throws BizException {
		MiResultOutParam<MiDirAutoCompOutParam> outParam = new MiResultOutParam<MiDirAutoCompOutParam>();
		outParam.setFg_deal(FBoolean.FALSE);
		outParam.setFg_HpSuccess(FBoolean.FALSE);
		outParam.setErrorMsg("医保目录自动对照 功能未实现。");
		return outParam;
	}

	/**
	 * 医保目录刷新对照
	 * @author zhang.hw
	 * @date 2019年7月18日
	 * @param param
	 * @return
	 * @throws BizException
	 */
	@Override
	public MiResultOutParam<MiDirRefreshCompOutParam> refreshCompMiDir(MiDirRefreshCompInParam param)
			throws BizException {
		MiResultOutParam<MiDirRefreshCompOutParam> outParam = new MiResultOutParam<MiDirRefreshCompOutParam>();
		outParam.setFg_deal(FBoolean.FALSE);
		outParam.setFg_HpSuccess(FBoolean.FALSE);
		outParam.setErrorMsg("医保目录刷新对照 功能未实现。");
		return outParam;
	}

	/**
	 * 目录保存对照
	 * @author zhang.hw
	 * @date 2019年7月18日
	 * @param param
	 * @return
	 * @throws BizException
	 */
	@Override
	public MiResultOutParam<MiSaveDirContrastOutParam> miSaveDirContrast(MiSaveDirContrastInParam param)
			throws BizException {
		MiResultOutParam<MiSaveDirContrastOutParam> outParam = new MiResultOutParam<MiSaveDirContrastOutParam>();
		outParam.setFg_deal(FBoolean.FALSE);
		outParam.setFg_HpSuccess(FBoolean.FALSE);
		outParam.setErrorMsg("目录保存对照  功能未实现。");
		return outParam;
	}

	/**
	 * 目录删除对照
	 * @author zhang.hw
	 * @date 2019年7月18日
	 * @param param
	 * @return
	 * @throws BizException
	 */
	@Override
	public MiResultOutParam<MiDelDirContrastOutParam> miDelDirContrast(MiDelDirContrastInParam param)
			throws BizException {
		MiResultOutParam<MiDelDirContrastOutParam> outParam = new MiResultOutParam<MiDelDirContrastOutParam>();
		outParam.setFg_deal(FBoolean.FALSE);
		outParam.setFg_HpSuccess(FBoolean.FALSE);
		outParam.setErrorMsg("目录删除对照  功能未实现。");
		return outParam;
	}
	
	/**
	 * 查询已对照的原始目录信息
	 * @author zhang.hw
	 * @date 2019年7月18日
	 * @param inParam
	 * @return
	 * @throws BizException
	 */
	@Override
	public MiResultOutParam<LoadOrginalDataOutParam> selectOrginalData(LoadOrginalDataInParam inParam) throws BizException {
		MiResultOutParam<LoadOrginalDataOutParam> outParam = new MiResultOutParam<LoadOrginalDataOutParam>();
		outParam.setFg_deal(FBoolean.FALSE);
		outParam.setFg_HpSuccess(FBoolean.FALSE);
		outParam.setErrorMsg("查询已对照的原始目录信息  功能未实现。");
		return outParam;
	}
}
