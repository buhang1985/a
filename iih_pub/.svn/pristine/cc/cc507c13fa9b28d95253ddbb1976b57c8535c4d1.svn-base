package iih.nm.nqm.nmnqmqcmt.i;

import iih.bd.bc.udi.pub.INmNurDictCodeTypeConst;
import xap.mw.core.data.Context;
import xap.wf.af.bpmn.ProcessDefinition;
import xap.wf.af.bpmn.SequenceFlow;
import xap.wf.af.bpmn.UserTaskActivity;
import xap.wf.af.contanier.ProDefsContainer;
import xap.wf.af.engine.ILogicDecision;
import xap.wf.af.model.WfUser;
import xap.wf.af.runtime.TaskInstance;
import xap.wf.af.server.WfFormInfoCtx;

public class NmnqmqcmtConditionNor implements ILogicDecision{

	@Override
	public boolean judge(TaskInstance arg0, SequenceFlow arg1, WfFormInfoCtx... arg2) {
		/*// TODO Auto-generated method stub
		String userId = Context.get().getUserId();
		
	    ProcessDefinition proDef = ProDefsContainer.getProDefByFlowTypePk(INmNurDictCodeTypeConst.NMNURLV_FLOW_TYPE_PK);
	    UserTaskActivity taskActivity_sen = proDef.getHumActs().get("3");
	    WfUser[] user_sen = taskActivity_sen.getAllotSgy().getActorSgy().getMainActor().getActors(arg2[0]);
	    
	    UserTaskActivity taskActivity_adv = proDef.getHumActs().get("9");
	    WfUser[] user_adv = taskActivity_adv.getAllotSgy().getActorSgy().getMainActor().getActors(arg2[0]);
	    //用户不是二三级审签用户，下流节点
		if(userId.equals(user_sen[0].getUserPk()) || userId.equals(user_adv[0].getUserPk())) {
			return false;
		}
	    */
		return true;
	}

}
