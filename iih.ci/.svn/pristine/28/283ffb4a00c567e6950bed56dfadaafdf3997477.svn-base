package iih.ci.ord.s.ems.itf.bp;

import iih.ci.ord.ems.d.CiEnContextDTO;
import iih.ci.ord.i.meta.DiagRstDTO;
import iih.ci.ord.i.meta.DiagSaveDTO;
import xap.mw.core.data.BizException;

/**
 * 诊断保存逻辑接口
 * 
 * @author wangqingzhu
 *
 */
public interface IDiagSaveBP {

	/**
	 * 诊断保存逻辑处理方法
	 * 
	 * @param diSaveInfo
	 * @return
	 */
	abstract DiagRstDTO save(CiEnContextDTO ctx, DiagSaveDTO diSaveInfo) throws BizException;
}
